'use strict';

var pwaInstallHandler = require('pwa-install-handler');
var react = require('react');

var usePWAInstall = function () {
    var subscribe = react.useCallback(function (onStoreChange) {
        pwaInstallHandler.pwaInstallHandler.addListener(onStoreChange);
        return function () {
            pwaInstallHandler.pwaInstallHandler.removeListener(onStoreChange);
        };
    }, []);
    var getSnapshot = react.useCallback(function () { return (pwaInstallHandler.pwaInstallHandler.canInstall() ? pwaInstallHandler.pwaInstallHandler.install : null); }, []);
    var getServerSnapshot = react.useCallback(function () { return null; }, []);
    return react.useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
};

exports.usePWAInstall = usePWAInstall;
//# sourceMappingURL=index.js.map
