import { DateTypeMap, IMenu } from './types';
declare const Menu: <T extends keyof DateTypeMap | undefined = "BS">({ type, show, bounds, portalRef, today, onChange, selectedDate, lang, menuContainerClassName, calendarClassName, portalClassName, components, converterMode, animation, max, min, }: IMenu<T>) => import("react/jsx-runtime").JSX.Element;
export default Menu;
